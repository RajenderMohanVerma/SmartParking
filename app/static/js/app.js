(() => { const root=document.documentElement; const saved=localStorage.getItem('smartpark-theme'); if(saved) root.dataset.theme=saved; const button=document.getElementById('themeToggle'); if(button) button.addEventListener('click',()=>{const next=root.dataset.theme==='dark'?'light':'dark'; root.dataset.theme=next; localStorage.setItem('smartpark-theme',next); button.querySelector('i').className=next==='dark'?'bi bi-sun':'bi bi-moon-stars'}); const token=document.querySelector('meta[name="csrf-token"]')?.content; document.querySelectorAll('form[method="post"]').forEach(form=>{if(token && !form.querySelector('input[name="csrf_token"]')){const input=document.createElement('input'); input.type='hidden'; input.name='csrf_token'; input.value=token; form.prepend(input)} form.addEventListener('submit',()=>{const submit=form.querySelector('button[type="submit"],button:not(.btn-close)'); if(submit && !submit.disabled){submit.disabled=true; submit.innerHTML='<span class="spinner-border spinner-border-sm me-2"></span>Working...'}})}); let installPrompt; const banner=document.getElementById('installBanner'); const installButton=document.getElementById('installApp'); const dismissButton=document.getElementById('dismissInstall'); const hideBanner=()=>{if(banner) banner.classList.add('d-none')}; window.addEventListener('beforeinstallprompt',event=>{event.preventDefault(); installPrompt=event; if(!sessionStorage.getItem('smartpark-install-dismissed')) banner?.classList.remove('d-none')}); installButton?.addEventListener('click',async()=>{if(!installPrompt)return; installPrompt.prompt(); await installPrompt.userChoice; installPrompt=null; hideBanner()}); dismissButton?.addEventListener('click',()=>{sessionStorage.setItem('smartpark-install-dismissed','1'); hideBanner()}); window.addEventListener('appinstalled',hideBanner); if('serviceWorker' in navigator) window.addEventListener('load',()=>navigator.serviceWorker.register('/sw.js',{scope:'/'}).catch(()=>{})); })();
